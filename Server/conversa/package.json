{
  "name": "conversa-server",
  "version": "0.1.0",
  "description": "Servidor de referência da conversa dinâmica (decisão 67) — o contrato do jogo na frente de qualquer modelo OpenAI-compatível.",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "test": "bun test",
    "typecheck": "bun x tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.5.0"
  }
}
