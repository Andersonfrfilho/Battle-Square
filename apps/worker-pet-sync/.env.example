API_BATTLE_PETS_URL=http://localhost:3100
SYNC_API_TOKEN=<mesmo valor do backend, escopo sync>
ED25519_PUBLIC_KEY_PEM="<chave pública correspondente à privada do backend>"
LOCAL_MIRROR_PATH=./data/pets-mirror.sqlite
MIRROR_ENCRYPTION_KEY=<32+ caracteres, ex.: openssl rand -hex 32>
SYNC_INTERVAL_SECONDS=30
